// The app compenent containing the header and the routing 

import { Component }          from '@angular/core';

@Component({
  selector: 'my-app',
  templateUrl: './app.component.html',
})
export class AppComponent {
  title = 'Inventory';
}
